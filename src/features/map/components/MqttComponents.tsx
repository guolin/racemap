'use client';
import { useEffect } from 'react';

interface MqttComponentsProps {
  // 添加需要的props
}

export default function MqttComponents(_props: MqttComponentsProps) {
  useEffect(() => {
    // MQTT相关初始化逻辑
  }, []);

  return null; // 这个组件只负责MQTT功能
} 